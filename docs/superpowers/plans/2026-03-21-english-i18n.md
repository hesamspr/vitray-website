# English Language Support Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add English language support to the Persian-only Vitray marketing website with an in-place language toggle, RTL↔LTR switching, and creative English translations.

**Architecture:** A lightweight `LanguageContext` (no library) manages `"fa"|"en"` state, persisted to `localStorage`. A `useTranslation` hook provides a `t(key)` lookup into `fa.json` / `en.json`. Direction and font family are toggled on `<html>` via class + attribute.

**Tech Stack:** React 18, TypeScript, Vite, Tailwind CSS, Vitest (added for unit tests)

---

## File Map

### New Files
| File | Purpose |
|------|---------|
| `web/src/context/LanguageContext.tsx` | Language state, localStorage, DOM side-effects |
| `web/src/hooks/useTranslation.ts` | `t(key)` dot-notation lookup |
| `web/src/locales/fa.json` | All Persian strings |
| `web/src/locales/en.json` | All English strings |
| `web/src/lib/navItems.ts` | Shared navItems (deduplicates the array repeated in every page) |
| `web/src/hooks/useTranslation.test.ts` | Unit tests for translation hook |

### Modified Files
| File | Change |
|------|--------|
| `web/package.json` | Add vitest |
| `web/vite.config.ts` | Add vitest config |
| `web/index.html` | Remove `dir="rtl"` `lang="fa"`, update `<title>` |
| `web/src/index.css` | Remove `html { direction: rtl }`, add `html.lang-en` font rules |
| `web/src/main.tsx` | Wrap with `<LanguageProvider>` |
| `web/src/lib/usePageTitle.ts` | Accept translation key, call `t()` |
| `web/src/components/ui/tubelight-navbar.tsx` | Add language toggle button |
| `web/src/components/ui/hero.tsx` | Replace hardcoded strings |
| `web/src/components/ui/cta-3.tsx` | Replace hardcoded strings |
| `web/src/components/ui/consultation-modal.tsx` | Replace hardcoded strings |
| `web/src/components/ui/footer-section.tsx` | Replace hardcoded strings |
| `web/src/components/ui/feature-section-with-card-gradient.tsx` | Replace hardcoded strings |
| `web/src/components/ui/plex-hero.tsx` | Replace hardcoded strings |
| `web/src/components/ui/pixel-hero.tsx` | Replace hardcoded strings |
| `web/src/components/ui/pulse-hero.tsx` | Replace hardcoded strings |
| `web/src/App.tsx` | Replace hardcoded strings, use `navItems` from lib |
| `web/src/pages/AboutPage.tsx` | Replace hardcoded strings |
| `web/src/pages/ContactPage.tsx` | Replace hardcoded strings |
| `web/src/pages/BiSolutionPage.tsx` | Replace hardcoded strings |
| `web/src/pages/PlexPage.tsx` | Replace hardcoded strings |
| `web/src/pages/PixelPage.tsx` | Replace hardcoded strings |
| `web/src/pages/PulsePage.tsx` | Replace hardcoded strings |
| All 7 dashboard pages | Replace hardcoded strings |

---

## Task 1: Add Vitest + Core i18n Files

**Files:**
- Create: `web/src/context/LanguageContext.tsx`
- Create: `web/src/hooks/useTranslation.ts`
- Create: `web/src/hooks/useTranslation.test.ts`
- Modify: `web/package.json`
- Modify: `web/vite.config.ts`

- [ ] **Step 1: Install vitest**

```bash
cd web && npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
```

- [ ] **Step 2: Add vitest config to `web/vite.config.ts`**

The project uses `@vitejs/plugin-react-swc` (not `@vitejs/plugin-react`). Add only the `test` block to the existing config — do NOT change the plugin:

```ts
// web/vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react-swc'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { '@': path.resolve(__dirname, './src') },
  },
  server: {
    port: 5173,
  },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: [],
  },
})
```

- [ ] **Step 3: Add test script to `web/package.json`**

In the `"scripts"` section, add:
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 4: Write the failing test for `useTranslation`**

Create `web/src/hooks/useTranslation.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { renderHook } from '@testing-library/react'
import React from 'react'
import { LanguageContext } from '../context/LanguageContext'
import { useTranslation } from './useTranslation'

const faDict = { nav: { home: 'خانه' }, nested: { deep: { key: 'مقدار' } } }
const enDict = { nav: { home: 'Home' }, nested: { deep: { key: 'Value' } } }

vi.mock('../locales/fa.json', () => ({ default: faDict }))
vi.mock('../locales/en.json', () => ({ default: enDict }))

function makeWrapper(lang: 'fa' | 'en') {
  return ({ children }: { children: React.ReactNode }) =>
    React.createElement(LanguageContext.Provider, { value: { lang, toggleLanguage: () => {} } }, children)
}

describe('useTranslation', () => {
  it('returns Persian string for fa locale', () => {
    const { result } = renderHook(() => useTranslation(), { wrapper: makeWrapper('fa') })
    expect(result.current.t('nav.home')).toBe('خانه')
  })

  it('returns English string for en locale', () => {
    const { result } = renderHook(() => useTranslation(), { wrapper: makeWrapper('en') })
    expect(result.current.t('nav.home')).toBe('Home')
  })

  it('supports deep nested keys', () => {
    const { result } = renderHook(() => useTranslation(), { wrapper: makeWrapper('en') })
    expect(result.current.t('nested.deep.key')).toBe('Value')
  })

  it('returns key string when key is missing', () => {
    const { result } = renderHook(() => useTranslation(), { wrapper: makeWrapper('en') })
    expect(result.current.t('nav.missing')).toBe('nav.missing')
  })
})
```

- [ ] **Step 5: Run test — expect it to error**

```bash
cd web && npm test
```

Expected: ERROR (module not found — `LanguageContext` and `useTranslation` don't exist yet). This is the expected TDD red state.

- [ ] **Step 6: Create `web/src/context/LanguageContext.tsx`**

```tsx
import { createContext, useContext, useEffect, useState, type ReactNode } from 'react'

type Lang = 'fa' | 'en'

interface LanguageContextValue {
  lang: Lang
  toggleLanguage: () => void
}

export const LanguageContext = createContext<LanguageContextValue>({
  lang: 'fa',
  toggleLanguage: () => {},
})

export function LanguageProvider({ children }: { children: ReactNode }) {
  const [lang, setLang] = useState<Lang>(() => {
    const saved = localStorage.getItem('vitray_lang')
    return saved === 'en' ? 'en' : 'fa'
  })

  useEffect(() => {
    const html = document.documentElement
    html.lang = lang
    html.dir = lang === 'en' ? 'ltr' : 'rtl'
    if (lang === 'en') {
      html.classList.add('lang-en')
    } else {
      html.classList.remove('lang-en')
    }
    localStorage.setItem('vitray_lang', lang)
  }, [lang])

  const toggleLanguage = () => setLang(prev => (prev === 'fa' ? 'en' : 'fa'))

  return (
    <LanguageContext.Provider value={{ lang, toggleLanguage }}>
      {children}
    </LanguageContext.Provider>
  )
}

export function useLanguage() {
  return useContext(LanguageContext)
}
```

- [ ] **Step 7: Create `web/src/hooks/useTranslation.ts`**

```ts
import fa from '../locales/fa.json'
import en from '../locales/en.json'
import { useLanguage } from '../context/LanguageContext'

type Dict = Record<string, unknown>

function lookup(dict: Dict, key: string): string {
  const parts = key.split('.')
  let current: unknown = dict
  for (const part of parts) {
    if (current == null || typeof current !== 'object') return key
    current = (current as Dict)[part]
  }
  if (typeof current === 'string') return current
  if (import.meta.env.DEV) console.warn(`[i18n] Missing translation key: "${key}"`)
  return key
}

export function useTranslation() {
  const { lang } = useLanguage()
  const dict = lang === 'en' ? (en as Dict) : (fa as Dict)
  return { t: (key: string) => lookup(dict, key), lang }
}
```

- [ ] **Step 8: Create stub locale files (will be filled in Task 2)**

Create `web/src/locales/fa.json`:
```json
{
  "nav": { "home": "خانه" }
}
```

Create `web/src/locales/en.json`:
```json
{
  "nav": { "home": "Home" }
}
```

- [ ] **Step 9: Run tests — expect them to pass**

```bash
cd web && npm test
```

Expected: 4 tests PASS

- [ ] **Step 10: Commit**

```bash
cd web && git add src/context/LanguageContext.tsx src/hooks/useTranslation.ts src/hooks/useTranslation.test.ts src/locales/fa.json src/locales/en.json package.json vite.config.ts && git commit -m "feat: add LanguageContext, useTranslation hook, and vitest setup"
```

---

## Task 2: Translation Files

**Files:**
- Modify: `web/src/locales/fa.json`
- Modify: `web/src/locales/en.json`

- [ ] **Step 1: Write complete `web/src/locales/fa.json`**

```json
{
  "nav": {
    "home": "خانه",
    "solutions": "راهکارها",
    "bi": "هوش تجاری",
    "plex": "پلکس",
    "pulse": "پالس",
    "pixel": "پیکسل",
    "academy": "آکادمی",
    "about": "درباره ما",
    "contact": "تماس با ما"
  },
  "page_titles": {
    "site_name": "راهکارهای ویترای",
    "home": "خانه",
    "about": "درباره ما",
    "contact": "تماس با ما",
    "plex": "پلکس",
    "pixel": "پیکسل",
    "pulse": "پالس",
    "bi_solution": "راهکار هوش تجاری",
    "dashboard_finance": "داشبورد مالی",
    "dashboard_hr": "داشبورد منابع انسانی",
    "dashboard_warehouse": "داشبورد انبار",
    "dashboard_maintenance": "داشبورد نگهداری و تعمیرات",
    "dashboard_production": "داشبورد تولید",
    "dashboard_distribution_sales": "داشبورد فروش پخش",
    "dashboard_b2b_sales": "داشبورد فروش B2B"
  },
  "hero": {
    "line1": "قبل از دیگران ببینید،",
    "line2": "قبل از دیگران اقدام کنید."
  },
  "cta": {
    "title": "این یک تغییر سخت نیست٬ یک شروع آسان است",
    "subtitle": "همین امروز با تیم فروش ما صحبت کنید و اولین قدم را بردارید.",
    "button": "درخواست مشاوره"
  },
  "modal": {
    "title": "درخواست مشاوره",
    "subtitle": "اطلاعات خود را وارد کنید، کارشناسان ما",
    "subtitle2": "به زودی با شما تماس می‌گیرند",
    "field_name": "نام و نام خانوادگی *",
    "field_name_placeholder": "علی احمدی",
    "field_mobile": "شماره موبایل *",
    "field_email": "ایمیل *",
    "field_company": "نام شرکت",
    "field_company_placeholder": "شرکت نمونه",
    "field_details": "توضیحات",
    "field_details_placeholder": "چطور می‌توانیم کمک کنیم؟",
    "submit": "ارسال درخواست",
    "submitting": "در حال ارسال...",
    "privacy": "اطلاعات شما محرمانه بوده و نزد ما محفوظ است.",
    "success_title": "درخواست ثبت شد",
    "success_body": "کارشناسان ما به زودی با شما تماس می‌گیرند.",
    "close": "بستن",
    "error_timeout": "زمان ارسال به پایان رسید. لطفاً دوباره تلاش کنید.",
    "error_generic": "خطایی رخ داد. لطفاً دوباره تلاش کنید."
  },
  "footer": {
    "solutions": "راهکارها",
    "company": "شرکت",
    "resources": "منابع",
    "social": "شبکه‌های اجتماعی",
    "bi": "هوش تجاری",
    "plex": "پلکس",
    "pulse": "پالس",
    "pixel": "پیکسل",
    "academy": "آکادمی",
    "about": "درباره ما",
    "contact": "تماس با ما",
    "blog": "وبلاگ",
    "linkedin": "لینکدین",
    "instagram": "اینستاگرام",
    "youtube": "یوتیوب",
    "aparat": "آپارات",
    "address": "تهران، پاسداران، نگارستان چهارم، پلاک ٬۲۸ واحد ۵",
    "copyright": "تمامی حقوق محفوظ است."
  },
  "home": {
    "customers_badge": "مشتریان ما",
    "customers_title": "برندهایی که به ما اعتماد کرده‌اند",
    "customers_body": "از تیم‌های کوچک تا سازمان‌های بزرگ، همه با ما رشد می‌کنند.",
    "solutions_badge": "راهکارها",
    "solutions_title": "ابزارهایی برای هر نیاز کسب‌وکار",
    "solutions_body": "مجموعه‌ای از راهکارها برای ساخت محصول، تحلیل داده و انتشار گزارش‌ها—از تیم‌های کوچک تا سازمان‌های بزرگ.",
    "bi_badge": "هوش تجاری",
    "bi_title": "هوش تجاری",
    "bi_body": "پیاده‌سازی کاملاً سفارشی هوش تجاری برای سازمان‌ها: مدل داده، یکپارچه‌سازی منابع، داشبوردهای مدیریتی و استانداردسازی گزارش‌ها با تمرکز بر کیفیت و حاکمیت داده.",
    "plex_badge": "بدون کد",
    "plex_title": "پلکس (Plex)",
    "plex_body": "ابزار توسعه اپلیکیشن بدون کدنویسی برای ساخت سریع پنل‌ها، فرم‌ها و گردش‌کارها—مناسب برای تیم‌هایی که سرعت اجرای ایده برایشان حیاتی است.",
    "pulse_badge": "اشتراک گذاری",
    "pulse_title": "پالس (Pulse)",
    "pulse_body": "پلتفرم فارسی برای انتشار و اشتراک‌گذاری گزارش‌ها و داشبوردهای BI؛ با مدیریت دسترسی، لینک‌های اشتراک امن و تجربه‌ای مناسب برای ارائه به مدیران و مشتریان.",
    "pixel_badge": "ابری",
    "pixel_title": "پیکسل (Pixel)",
    "pixel_body": "راهکار اقتصادی و میزبانی‌شده در فضای ابری برای کسب‌وکارهای کوچک؛ با راه‌اندازی سریع، هزینه نگهداری پایین و داشبوردهای آماده برای تصمیم‌گیری روزانه.",
    "academy_badge": "آموزش",
    "academy_title": "آکادمی ویترای",
    "academy_body": "دوره‌های تخصصی Power BI برای تحلیلگران داده و کارشناسان BI؛ از مقدماتی تا پیشرفته، با مدرک معتبر و پشتیبانی مستمر.",
    "learn_more": "بیشتر بدانید",
    "why_badge": "چرا ویترای؟",
    "why_title": "متفاوت با دیگران",
    "why_body": "ترکیب تجربه، فناوری و تمرکز بر نتیجه، ما را به شریک مطمئن کسب‌وکارها تبدیل کرده است.",
    "why_business": "آشنایی با کسب‌وکارها",
    "why_business_body": "تیم ما با سال‌ها حضور در صنایع مختلف و همکاری با سازمان‌های گوناگون، به‌خوبی با چالش‌ها، نیازها و فرصت‌هایی که در مسیر رشد پیش می‌آیند، آشناست.",
    "why_speed": "سرعت اجرا",
    "why_speed_body": "تجربه گسترده ما در اجرای پروژه‌های متنوع هوش تجاری، برگ برنده‌ای است که به شما کمک می‌کند با سرعت و دقت به نتایج مورد انتظار دست یابید.",
    "why_customer": "مشتری‌مداری",
    "why_customer_body": "در ویترای، موفقیت مشتریانمان نه تنها هدف، بلکه ارزش محوری ماست. رشد و دستاوردهای شما مستقیماً به پیشرفت ما گره خورده است.",
    "why_tools": "ابزار و رویکرد درست",
    "why_tools_body": "رویکرد ما با Semantic Layer مستقل روی SSAS Tabular و فرآیند ETL پیشرفته، نیازهای سازمان‌های بزرگ را فراتر از محدودیت‌های معمول پاسخ می‌دهد.",
    "success_badge": "موفقیت مشتریان",
    "success_title": "نتایج واقعی برای کسب‌وکارهای واقعی",
    "success_body": "بیش از ۱۵۰ سازمان در صنایع مختلف با ویترای به داده‌محوری رسیده‌اند."
  },
  "about": {
    "badge": "درباره ما",
    "hero_title": "داده را به تصمیم تبدیل می‌کنیم",
    "hero_body": "شرکت ویترای با هدف کمک به سازمان‌ها در تبدیل داده‌های خام به بینش‌های عملی و قابل اجرا در سال ۱۳۹۸ تأسیس شده است. تیم ما متشکل از متخصصان حوزه‌های فناوری و کسب‌وکار است که راه‌حل‌های نوآورانه هوش تجاری را ارائه می‌دهند.",
    "what_badge": "ما چه می‌کنیم",
    "what_title": "هوش تجاری در خدمت سازمان شما",
    "what_body1": "با بهره‌گیری از ابزارهای پیشرفته‌ای همچون Microsoft Power BI و دیگر فناوری‌های تحلیلی، داده‌های شما را به گزارش‌ها و داشبوردهای بصری تبدیل می‌کنیم تا تصمیم‌گیری سریع‌تر، دقیق‌تر و مبتنی بر داده ممکن شود.",
    "what_body2": "تمرکز ما بر توانمندسازی سازمان‌ها برای پیشرفت در بازارهای رقابتی است. ما برای هر صنعت راهکارهای اختصاصی ارائه می‌دهیم تا نیازهای خاص آن را پوشش دهیم.",
    "what_body3": "هدف نهایی ویترای ایجاد تحول دیجیتال و تقویت نوآوری‌های مبتنی بر داده در سازمان‌هاست.",
    "stat_founded": "سال تأسیس",
    "stat_projects": "پروژه موفق"
  },
  "contact": {
    "badge": "تماس با ما",
    "title": "با ما در ارتباط باشید",
    "body": "برای دریافت مشاوره رایگان یا هر سوالی درباره محصولات ویترای، پیام بگذارید.",
    "about_label": "تلفن",
    "address_label": "آدرس",
    "email_label": "ایمیل",
    "address_value": "تهران، پاسداران، نگارستان چهارم، پلاک ٬۲۸ واحد ۵",
    "company_blurb": "ویترای با هدف کمک به سازمان‌ها در تبدیل داده‌های خام به بینش‌های عملی تأسیس شده است. تیم ما از متخصصان فناوری و کسب‌وکار تشکیل شده که راه‌حل‌های نوآورانه هوش تجاری ارائه می‌دهند.",
    "form_name": "نام و نام خانوادگی",
    "form_mobile": "شماره موبایل",
    "form_email": "ایمیل",
    "form_company": "نام شرکت",
    "form_details": "توضیحات",
    "form_name_placeholder": "علی احمدی",
    "form_company_placeholder": "شرکت نمونه",
    "form_submit": "ارسال پیام",
    "form_submitting": "در حال ارسال...",
    "success_title": "پیام شما ارسال شد",
    "success_body": "به زودی با شما تماس می‌گیریم."
  },
  "plex_hero": {
    "badge": "پلکس (Plex)",
    "title1": "یک پلتفرم،",
    "title2": "بی‌نهایت راه‌حل",
    "body": "در کوتاه‌ترین زمان، اپلیکیشن‌های سازمانی، گردش‌کارهای هوشمند و راهکارهای سفارشی بسازید — بدون نیاز به تیم توسعه یا پیچیدگی‌های فنی."
  },
  "dashboards": {
    "b2b_title": "فروش B2B",
    "b2b_desc": "پیگیری فرصت‌های فروش، عملکرد تیم فروش و تحلیل مشتریان سازمانی در یک نگاه.",
    "dist_title": "فروش پخش",
    "dist_desc": "مدیریت کانال‌های توزیع، عملکرد نمایندگان و تحلیل فروش منطقه‌ای.",
    "finance_title": "مالی",
    "finance_desc": "نظارت بر جریان نقدی، سودآوری، بودجه و گزارش‌های مالی جامع.",
    "hr_title": "منابع انسانی",
    "hr_desc": "پیگیری عملکرد کارکنان، حضور و غیاب، آموزش و توسعه سازمانی.",
    "warehouse_title": "انبار",
    "warehouse_desc": "مدیریت موجودی، نقطه سفارش‌گذاری و تحلیل گردش کالا در انبار.",
    "production_title": "تولید",
    "production_desc": "پایش خط تولید، نرخ ضایعات، بهره‌وری ماشین‌آلات و شاخص OEE.",
    "maintenance_title": "نگهداری و تعمیرات",
    "maintenance_desc": "برنامه‌ریزی نگهداری پیشگیرانه، پیگیری خرابی‌ها و هزینه‌های تعمیرات.",
    "related_badge": "سایر داشبوردها",
    "related_title": "سایر نمونه داشبوردها",
    "related_body": "قالب‌های داشبورد اختصاصی برای سایر صنایع را نیز مشاهده کنید."
  }
}
```

- [ ] **Step 2: Write complete `web/src/locales/en.json`**

```json
{
  "nav": {
    "home": "Home",
    "solutions": "Solutions",
    "bi": "Business Intelligence",
    "plex": "Plex",
    "pulse": "Pulse",
    "pixel": "Pixel",
    "academy": "Academy",
    "about": "About",
    "contact": "Contact"
  },
  "page_titles": {
    "site_name": "Vitray Solutions",
    "home": "Home",
    "about": "About Us",
    "contact": "Contact",
    "plex": "Plex",
    "pixel": "Pixel",
    "pulse": "Pulse",
    "bi_solution": "Business Intelligence Solution",
    "dashboard_finance": "Finance Dashboard",
    "dashboard_hr": "HR Dashboard",
    "dashboard_warehouse": "Warehouse Dashboard",
    "dashboard_maintenance": "Maintenance Dashboard",
    "dashboard_production": "Production Dashboard",
    "dashboard_distribution_sales": "Distribution Sales Dashboard",
    "dashboard_b2b_sales": "B2B Sales Dashboard"
  },
  "hero": {
    "line1": "See it before anyone else.",
    "line2": "Act before anyone else."
  },
  "cta": {
    "title": "This isn't a hard change — it's an easy start.",
    "subtitle": "Talk to our team today and take the first step.",
    "button": "Book a Consultation"
  },
  "modal": {
    "title": "Book a Consultation",
    "subtitle": "Fill in your details and one of our experts",
    "subtitle2": "will reach out shortly",
    "field_name": "Full Name *",
    "field_name_placeholder": "Alex Johnson",
    "field_mobile": "Mobile Number *",
    "field_email": "Email *",
    "field_company": "Company Name",
    "field_company_placeholder": "Acme Corp",
    "field_details": "Details",
    "field_details_placeholder": "How can we help?",
    "submit": "Submit Request",
    "submitting": "Sending...",
    "privacy": "Your information is confidential and secure with us.",
    "success_title": "Request Received",
    "success_body": "Our team will be in touch with you shortly.",
    "close": "Close",
    "error_timeout": "Request timed out. Please try again.",
    "error_generic": "Something went wrong. Please try again."
  },
  "footer": {
    "solutions": "Solutions",
    "company": "Company",
    "resources": "Resources",
    "social": "Social",
    "bi": "Business Intelligence",
    "plex": "Plex",
    "pulse": "Pulse",
    "pixel": "Pixel",
    "academy": "Academy",
    "about": "About",
    "contact": "Contact",
    "blog": "Blog",
    "linkedin": "LinkedIn",
    "instagram": "Instagram",
    "youtube": "YouTube",
    "aparat": "Aparat",
    "address": "Unit 5, No. 28, Negarestan 4th, Pasdaran, Tehran",
    "copyright": "All rights reserved."
  },
  "home": {
    "customers_badge": "Our Clients",
    "customers_title": "Brands That Trust Us",
    "customers_body": "From lean startups to large enterprises — everyone grows with Vitray.",
    "solutions_badge": "Solutions",
    "solutions_title": "Tools Built for Every Business Need",
    "solutions_body": "A suite of solutions for building products, analyzing data, and distributing reports — from small teams to large organizations.",
    "bi_badge": "Business Intelligence",
    "bi_title": "Business Intelligence",
    "bi_body": "Fully custom BI implementation for enterprises: data modeling, source integration, executive dashboards, and standardized reporting — built around data quality and governance.",
    "plex_badge": "No-Code",
    "plex_title": "Plex",
    "plex_body": "A no-code application builder for rapidly creating panels, forms, and workflows — built for teams where speed of execution is everything.",
    "pulse_badge": "Sharing",
    "pulse_title": "Pulse",
    "pulse_body": "A platform for publishing and sharing BI reports and dashboards — with access management, secure share links, and a polished experience for presenting to executives and clients.",
    "pixel_badge": "Cloud",
    "pixel_title": "Pixel",
    "pixel_body": "An affordable, cloud-hosted BI solution for small businesses — fast setup, low maintenance, and ready-made dashboards for daily decision-making.",
    "academy_badge": "Training",
    "academy_title": "Vitray Academy",
    "academy_body": "Specialized Power BI courses for data analysts and BI professionals — from beginner to advanced, with recognized certification and ongoing support.",
    "learn_more": "Learn More",
    "why_badge": "Why Vitray?",
    "why_title": "Built Different",
    "why_body": "The combination of deep experience, modern tooling, and a relentless focus on outcomes makes us the partner businesses rely on.",
    "why_business": "Industry Knowledge",
    "why_business_body": "Years working across diverse industries means our team understands the real challenges, needs, and opportunities that come with growth — not just the theory.",
    "why_speed": "Execution Speed",
    "why_speed_body": "Extensive experience across varied BI projects means you reach expected outcomes faster and more reliably than with a generalist team.",
    "why_customer": "Client Success",
    "why_customer_body": "At Vitray, our clients' success isn't just a goal — it's our core value. Your growth and achievements are directly tied to ours.",
    "why_tools": "The Right Approach",
    "why_tools_body": "Our independent Semantic Layer on SSAS Tabular and advanced ETL pipeline answers the needs of large organizations beyond typical constraints.",
    "success_badge": "Customer Success",
    "success_title": "Real Results for Real Businesses",
    "success_body": "Over 150 organizations across industries have become data-driven with Vitray."
  },
  "about": {
    "badge": "About Us",
    "hero_title": "We Turn Data Into Decisions",
    "hero_body": "Vitray was founded in 2019 with one goal: help organizations transform raw data into actionable insight. Our team brings together technology and business specialists who deliver innovative BI solutions.",
    "what_badge": "What We Do",
    "what_title": "Business Intelligence That Works for You",
    "what_body1": "Using advanced tools like Microsoft Power BI and other analytics technologies, we transform your data into visual reports and dashboards — enabling faster, more accurate, data-driven decisions.",
    "what_body2": "We focus on empowering organizations to thrive in competitive markets, delivering tailored solutions for every industry's unique requirements.",
    "what_body3": "Vitray's ultimate goal is to drive digital transformation and strengthen data-driven innovation across organizations.",
    "stat_founded": "Founded",
    "stat_projects": "Successful Projects"
  },
  "contact": {
    "badge": "Contact",
    "title": "Get in Touch",
    "body": "For a free consultation or any questions about Vitray products, drop us a message.",
    "about_label": "Phone",
    "address_label": "Address",
    "email_label": "Email",
    "address_value": "Unit 5, No. 28, Negarestan 4th, Pasdaran, Tehran, Iran",
    "company_blurb": "Vitray was founded to help organizations turn raw data into actionable insight. Our team of technology and business specialists delivers innovative BI solutions.",
    "form_name": "Full Name",
    "form_mobile": "Mobile Number",
    "form_email": "Email",
    "form_company": "Company Name",
    "form_details": "Message",
    "form_name_placeholder": "Alex Johnson",
    "form_company_placeholder": "Acme Corp",
    "form_submit": "Send Message",
    "form_submitting": "Sending...",
    "success_title": "Message Sent",
    "success_body": "We'll be in touch with you shortly."
  },
  "plex_hero": {
    "badge": "Plex",
    "title1": "One platform.",
    "title2": "Infinite solutions.",
    "body": "Build enterprise applications, smart workflows, and custom solutions in record time — no development team or technical complexity required."
  },
  "dashboards": {
    "b2b_title": "B2B Sales",
    "b2b_desc": "Track sales opportunities, team performance, and enterprise client analysis at a glance.",
    "dist_title": "Distribution Sales",
    "dist_desc": "Manage distribution channels, agent performance, and regional sales analysis.",
    "finance_title": "Finance",
    "finance_desc": "Monitor cash flow, profitability, budgets, and comprehensive financial reports.",
    "hr_title": "Human Resources",
    "hr_desc": "Track employee performance, attendance, training, and organizational development.",
    "warehouse_title": "Warehouse",
    "warehouse_desc": "Manage inventory, reorder points, and analyze stock turnover.",
    "production_title": "Production",
    "production_desc": "Monitor production lines, waste rates, machine efficiency, and OEE metrics.",
    "maintenance_title": "Maintenance",
    "maintenance_desc": "Plan preventive maintenance, track breakdowns, and manage repair costs.",
    "related_badge": "More Dashboards",
    "related_title": "Other Dashboard Templates",
    "related_body": "Explore specialized dashboard templates for other industries."
  }
}
```

- [ ] **Step 3: Run tests to confirm they still pass**

```bash
cd web && npm test
```

Expected: PASS (stubs replaced with real content)

- [ ] **Step 4: Commit**

```bash
cd web && git add src/locales/ && git commit -m "feat: add complete fa/en translation files"
```

---

## Task 3: Bootstrap — index.html, index.css, main.tsx

**Files:**
- Modify: `web/index.html`
- Modify: `web/src/index.css`
- Modify: `web/src/main.tsx`

- [ ] **Step 1: Update `web/index.html`**

Replace:
```html
<html lang="fa" dir="rtl">
```
With:
```html
<html lang="fa" dir="rtl">
```
Wait — we keep the default attributes so SSR/initial load works for Persian users. Then JS takes over. Change only the `<title>`:

Replace:
```html
<title>راهکارهای ویترای</title>
```
With:
```html
<title>Vitray</title>
```

- [ ] **Step 2: Update `web/src/index.css`**

Remove the standalone `html { direction: rtl; }` block inside `@layer base` (lines 246–248):
```css
/* REMOVE THIS BLOCK: */
html {
  direction: rtl;
}
```

Add English font override rules anywhere after the existing `body` rule:
```css
/* English language font override */
html.lang-en body {
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

html.lang-en h1,
html.lang-en h2,
html.lang-en h3,
html.lang-en h4,
html.lang-en h5,
html.lang-en h6 {
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}
```

- [ ] **Step 3: Update `web/src/main.tsx`**

Add import and wrap `<BrowserRouter>` with `<LanguageProvider>`:

```tsx
import React, { Suspense, lazy } from 'react';
import ReactDOM from 'react-dom/client';
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { LanguageProvider } from './context/LanguageContext';
import { App } from './App';
import { ScrollToTop } from './components/ScrollToTop';
import './index.css';

const AboutPage = lazy(() => import('./pages/AboutPage').then(m => ({ default: m.AboutPage })));
const ContactPage = lazy(() => import('./pages/ContactPage').then(m => ({ default: m.ContactPage })));
const BiSolutionPage = lazy(() => import('./pages/BiSolutionPage').then(m => ({ default: m.BiSolutionPage })));
const PlexPage = lazy(() => import('./pages/PlexPage').then(m => ({ default: m.PlexPage })));
const PixelPage = lazy(() => import('./pages/PixelPage').then(m => ({ default: m.PixelPage })));
const PulsePage = lazy(() => import('./pages/PulsePage').then(m => ({ default: m.PulsePage })));
const B2BSalesDashboardPage = lazy(() => import('./pages/dashboards/B2BSalesDashboardPage').then(m => ({ default: m.B2BSalesDashboardPage })));
const DistributionSalesDashboardPage = lazy(() => import('./pages/dashboards/DistributionSalesDashboardPage').then(m => ({ default: m.DistributionSalesDashboardPage })));
const FinanceDashboardPage = lazy(() => import('./pages/dashboards/FinanceDashboardPage').then(m => ({ default: m.FinanceDashboardPage })));
const HRDashboardPage = lazy(() => import('./pages/dashboards/HRDashboardPage').then(m => ({ default: m.HRDashboardPage })));
const WarehouseDashboardPage = lazy(() => import('./pages/dashboards/WarehouseDashboardPage').then(m => ({ default: m.WarehouseDashboardPage })));
const ProductionDashboardPage = lazy(() => import('./pages/dashboards/ProductionDashboardPage').then(m => ({ default: m.ProductionDashboardPage })));
const MaintenanceDashboardPage = lazy(() => import('./pages/dashboards/MaintenanceDashboardPage').then(m => ({ default: m.MaintenanceDashboardPage })));

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <LanguageProvider>
      <BrowserRouter>
        <ScrollToTop />
        <Suspense fallback={null}>
          <Routes>
            <Route path="/" element={<App />} />
            <Route path="/about" element={<AboutPage />} />
            <Route path="/contact" element={<ContactPage />} />
            <Route path="/bi-solution" element={<BiSolutionPage />} />
            <Route path="/plex" element={<PlexPage />} />
            <Route path="/pixel" element={<PixelPage />} />
            <Route path="/pulse" element={<PulsePage />} />
            <Route path="/bi-dashboards/b2b-sales" element={<B2BSalesDashboardPage />} />
            <Route path="/bi-dashboards/distribution-sales" element={<DistributionSalesDashboardPage />} />
            <Route path="/bi-dashboards/finance" element={<FinanceDashboardPage />} />
            <Route path="/bi-dashboards/hr" element={<HRDashboardPage />} />
            <Route path="/bi-dashboards/warehouse" element={<WarehouseDashboardPage />} />
            <Route path="/bi-dashboards/production" element={<ProductionDashboardPage />} />
            <Route path="/bi-dashboards/maintenance" element={<MaintenanceDashboardPage />} />
          </Routes>
        </Suspense>
      </BrowserRouter>
    </LanguageProvider>
  </React.StrictMode>,
);
```

- [ ] **Step 4: Verify build**

```bash
cd web && npm run build
```

Expected: Build succeeds with no errors.

- [ ] **Step 5: Commit**

```bash
cd web && git add index.html src/index.css src/main.tsx && git commit -m "feat: bootstrap i18n — wrap app with LanguageProvider, fix CSS direction"
```

---

## Task 4: Shared navItems + usePageTitle

**Files:**
- Create: `web/src/lib/navItems.ts`
- Modify: `web/src/lib/usePageTitle.ts`

The `navItems` array is duplicated identically in 10+ files. Extract it once.

- [ ] **Step 1: Create `web/src/lib/navItems.ts`**

```ts
import { Home, Info, Mail, Package } from 'lucide-react'
import { BarChart3, Cloud, GraduationCap, Layers3, Share2 } from 'lucide-react'

// These are intentionally defined as plain objects (not using t()) because the
// NavBar component receives them as props from each page. The page components
// will call useTranslation() and pass translated names via getNavItems().

const BiNavIcon = ({ className }: { className?: string }) => (
  <img src="/fav.png" alt="BI" className={className} />
)
const PlexNavIcon = ({ className }: { className?: string }) => (
  <img src="/product%20logos/plex%20fav%20white.png" alt="Plex" className={className} />
)
const PixelNavIcon = ({ className }: { className?: string }) => (
  <img src="/product%20logos/Pixel%20Fav%20W.png" alt="Pixel" className={className} />
)
const PulseNavIcon = ({ className }: { className?: string }) => (
  <img src="/product%20logos/Pulse%20Fav%20W.png" alt="Pulse" className={className} />
)
const AcademyNavIcon = ({ className }: { className?: string }) => (
  <img src="/product%20logos/Academy%20FAV%20White.png" alt="Academy" className={className} />
)

export function getNavItems(t: (key: string) => string) {
  return [
    { name: t('nav.home'), url: '/', icon: Home },
    {
      name: t('nav.solutions'),
      url: '/#products',
      icon: Package,
      subItems: [
        { name: t('nav.bi'), url: '/bi-solution', icon: BiNavIcon },
        { name: t('nav.plex'), url: '/plex', icon: PlexNavIcon },
        { name: t('nav.pulse'), url: '/pulse', icon: PulseNavIcon },
        { name: t('nav.pixel'), url: '/pixel', icon: PixelNavIcon },
        { name: t('nav.academy'), url: 'https://academy.vitrayco.com', icon: AcademyNavIcon },
      ],
    },
    { name: t('nav.about'), url: '/about', icon: Info },
    { name: t('nav.contact'), url: '/contact', icon: Mail },
  ]
}
```

> **Note for App.tsx:** App.tsx uses `url: '#home'` for the home link AND `url: '#products'` for the solutions link (hash scroll, no page reload). All other pages use `url: '/'` and `url: '/#products'`. The `getNavItems` function uses `'/'` and `'/#products'`. When using `getNavItems` in `App.tsx`, override BOTH: the home item url to `'#home'` and the solutions item url to `'#products'`.

- [ ] **Step 2: Update `web/src/lib/usePageTitle.ts`**

```ts
import { useEffect } from 'react'
import { useTranslation } from '@/hooks/useTranslation'

export function usePageTitle(pageKey?: string) {
  const { t, lang } = useTranslation()

  useEffect(() => {
    const siteName = t('page_titles.site_name')
    document.title = pageKey ? `${siteName} - ${t(pageKey)}` : siteName
    return () => {
      document.title = t('page_titles.site_name')
    }
  // Use `lang` (not `t`) as dependency — `t` is recreated each render but `lang` only changes on toggle
  }, [pageKey, lang]) // eslint-disable-line react-hooks/exhaustive-deps
}
```

- [ ] **Step 3: Verify build**

```bash
cd web && npm run build
```

Expected: Build succeeds.

- [ ] **Step 4: Commit**

```bash
cd web && git add src/lib/navItems.ts src/lib/usePageTitle.ts && git commit -m "feat: extract shared navItems, update usePageTitle to use translations"
```

---

## Task 5: NavBar Language Switcher

**Files:**
- Modify: `web/src/components/ui/tubelight-navbar.tsx`

- [ ] **Step 1: Update `web/src/components/ui/tubelight-navbar.tsx`**

Add `useLanguage` import and insert toggle button into the navbar. The toggle sits at the end of the nav, after the items loop:

```tsx
import React, { useEffect, useRef, useState } from "react"
import { motion, AnimatePresence } from "framer-motion"
import { Link, useLocation } from "react-router-dom"
import { ChevronDown, LucideIcon } from "lucide-react"
import { cn } from "@/lib/utils"
import { useLanguage } from "@/context/LanguageContext"

interface NavSubItem {
  name: string
  url: string
  icon?: React.ComponentType<{ className?: string }>
}

interface NavItem {
  name: string
  url: string
  icon: LucideIcon
  subItems?: NavSubItem[]
}

interface NavBarProps {
  items: NavItem[]
  className?: string
}

function GlowIndicator() {
  return (
    <div className="absolute inset-0 w-full bg-primary/5 rounded-full -z-10 animate-in fade-in duration-200">
      <div className="absolute -top-2 left-1/2 -translate-x-1/2 w-8 h-1 bg-primary rounded-t-full">
        <div className="absolute w-12 h-6 bg-primary/20 rounded-full blur-md -top-2 -left-2" />
        <div className="absolute w-8 h-6 bg-primary/20 rounded-full blur-md -top-1" />
        <div className="absolute w-4 h-4 bg-primary/20 rounded-full blur-sm top-0 left-2" />
      </div>
    </div>
  )
}

export function NavBar({ items, className }: NavBarProps) {
  const { pathname } = useLocation()
  const { lang, toggleLanguage } = useLanguage()

  const findActiveItem = (path: string) =>
    items.find(item =>
      item.url === path ||
      item.subItems?.some(sub => sub.url === path)
    )?.name ?? items[0].name

  const [activeTab, setActiveTab] = useState(() => findActiveItem(pathname))
  const [openDropdown, setOpenDropdown] = useState<string | null>(null)
  const navRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    setActiveTab(findActiveItem(pathname))
  }, [pathname, items])

  useEffect(() => {
    const handleClickOutside = (e: MouseEvent) => {
      if (navRef.current && !navRef.current.contains(e.target as Node)) {
        setOpenDropdown(null)
      }
    }
    document.addEventListener("mousedown", handleClickOutside)
    return () => document.removeEventListener("mousedown", handleClickOutside)
  }, [])

  const itemClass = (isActive: boolean) =>
    cn(
      "relative cursor-pointer text-sm font-semibold px-5 py-2 rounded-full transition-colors flex items-center gap-1.5",
      "text-foreground/80 hover:text-primary",
      isActive && "text-primary",
    )

  return (
    <div className={cn("fixed top-0 left-1/2 -translate-x-1/2 z-50 pt-4", className)}>
      <div
        ref={navRef}
        className="relative flex items-center gap-1 bg-background/10 border border-border/50 backdrop-blur-lg py-1 px-1 rounded-full shadow-lg"
      >
        <a href="/" className="px-3 py-1 flex items-center">
          <img src="/Vitray.png" alt="Vitray" className="h-6 w-auto" />
        </a>
        {items.map((item) => {
          const Icon = item.icon
          const isActive = activeTab === item.name
          const hasDropdown = !!item.subItems?.length
          const isOpen = openDropdown === item.name
          const isRouterLink = !hasDropdown && !item.url.startsWith('#')

          return (
            <div key={item.name} className="relative">
              {hasDropdown ? (
                <button
                  onClick={() => setOpenDropdown(isOpen ? null : item.name)}
                  className={itemClass(isActive)}
                >
                  <span className="hidden md:inline">{item.name}</span>
                  <ChevronDown
                    size={13}
                    className={cn("hidden md:inline transition-transform duration-200", isOpen && "rotate-180")}
                  />
                  <span className="md:hidden"><Icon size={18} strokeWidth={2.5} /></span>
                  {isActive && <GlowIndicator />}
                </button>
              ) : isRouterLink ? (
                <Link
                  to={item.url}
                  onClick={() => { setActiveTab(item.name); setOpenDropdown(null) }}
                  className={itemClass(isActive)}
                >
                  <span className="hidden md:inline">{item.name}</span>
                  <span className="md:hidden"><Icon size={18} strokeWidth={2.5} /></span>
                  {isActive && <GlowIndicator />}
                </Link>
              ) : (
                <a
                  href={item.url}
                  onClick={() => { setActiveTab(item.name); setOpenDropdown(null) }}
                  className={itemClass(isActive)}
                >
                  <span className="hidden md:inline">{item.name}</span>
                  <span className="md:hidden"><Icon size={18} strokeWidth={2.5} /></span>
                  {isActive && <GlowIndicator />}
                </a>
              )}

              {hasDropdown && (
                <AnimatePresence>
                  {isOpen && (
                    <motion.div
                      initial={{ opacity: 0, y: -6, scale: 0.97 }}
                      animate={{ opacity: 1, y: 0, scale: 1 }}
                      exit={{ opacity: 0, y: -6, scale: 0.97 }}
                      transition={{ duration: 0.15 }}
                      className="absolute top-full mt-2 right-0 min-w-[190px] bg-background/95 border border-border/60 backdrop-blur-lg rounded-2xl shadow-xl py-1.5 overflow-hidden"
                    >
                      {item.subItems!.map((sub) => {
                        const isExternal = sub.url.includes('#') || sub.url.startsWith('http')
                        const SubComp = isExternal ? 'a' : Link
                        const linkProp = isExternal
                          ? { href: sub.url, ...(sub.url.startsWith('http') ? { target: '_blank', rel: 'noopener noreferrer' } : {}) }
                          : { to: sub.url }
                        return (
                          <SubComp
                            key={sub.name}
                            {...(linkProp as any)}
                            onClick={() => { setActiveTab(item.name); setOpenDropdown(null) }}
                            className="flex items-center gap-2.5 px-4 py-2.5 text-sm text-foreground/80 hover:text-primary hover:bg-muted/50 transition-colors"
                          >
                            {sub.icon && <sub.icon className="h-3.5 w-3.5 shrink-0 object-contain" />}
                            {sub.name}
                          </SubComp>
                        )
                      })}
                    </motion.div>
                  )}
                </AnimatePresence>
              )}
            </div>
          )
        })}

        {/* Language toggle — desktop */}
        <button
          onClick={toggleLanguage}
          className="hidden md:flex items-center px-3 py-1.5 rounded-full text-xs font-semibold border border-border/50 text-foreground/70 hover:text-primary hover:border-primary/50 transition-colors ms-1"
          aria-label="Toggle language"
        >
          {lang === 'fa' ? 'EN' : 'فا'}
        </button>

        {/* Language toggle — mobile (visible in mobile bar) */}
        <button
          onClick={toggleLanguage}
          className="flex md:hidden items-center px-3 py-1.5 rounded-full text-xs font-semibold border border-border/50 text-foreground/70 hover:text-primary hover:border-primary/50 transition-colors ms-1"
          aria-label="Toggle language"
        >
          {lang === 'fa' ? 'EN' : 'فا'}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verify build**

```bash
cd web && npm run build
```

Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
cd web && git add src/components/ui/tubelight-navbar.tsx && git commit -m "feat: add language toggle button to navbar"
```

---

## Task 6: hero.tsx + cta-3.tsx

**Files:**
- Modify: `web/src/components/ui/hero.tsx`
- Modify: `web/src/components/ui/cta-3.tsx`

- [ ] **Step 1: Update `web/src/components/ui/hero.tsx`**

```tsx
import { ShaderAnimation } from "@/components/ui/shader-animation"
import { useTranslation } from "@/hooks/useTranslation"

export const PremiumHero = () => {
  const { t } = useTranslation()

  return (
    <div className="relative h-screen w-full overflow-hidden">
      <ShaderAnimation />
      <div className="relative z-20 flex h-screen w-full items-center justify-center px-6 text-center">
        <h1 className="text-5xl font-bold tracking-tight text-white md:text-7xl flex flex-col gap-6">
          <span>{t('hero.line1')}</span>
          <span>{t('hero.line2')}</span>
        </h1>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Update `web/src/components/ui/cta-3.tsx`**

```tsx
import { useState } from 'react'
import { PlusIcon } from "lucide-react"
import { Button } from "@/components/ui/button"
import { ConsultationModal } from "@/components/ui/consultation-modal"
import { ShineBorder } from "@/components/ui/shine-border"
import { useTranslation } from "@/hooks/useTranslation"

export function CallToAction() {
  const [modalOpen, setModalOpen] = useState(false)
  const { t } = useTranslation()

  return (
    <>
      <div className="relative mx-auto flex w-full max-w-3xl flex-col justify-between gap-y-6 border-y border-border bg-[radial-gradient(35%_80%_at_25%_0%,hsl(var(--foreground)/0.08),transparent)] px-4 py-8">
        <PlusIcon className="absolute top-[-12.5px] left-[-11.5px] z-10 size-6" strokeWidth={1} />
        <PlusIcon className="absolute top-[-12.5px] right-[-11.5px] z-10 size-6" strokeWidth={1} />
        <PlusIcon className="absolute bottom-[-12.5px] left-[-11.5px] z-10 size-6" strokeWidth={1} />
        <PlusIcon className="absolute right-[-11.5px] bottom-[-12.5px] z-10 size-6" strokeWidth={1} />
        <div className="pointer-events-none absolute -top-6 -bottom-6 left-0 w-px border-l border-border" />
        <div className="pointer-events-none absolute -top-6 -bottom-6 right-0 w-px border-r border-border" />
        <div className="pointer-events-none absolute -z-10 top-0 left-1/2 h-full border-l border-dashed border-border" />

        <div className="space-y-1">
          <h2 className="text-center font-bold text-2xl">{t('cta.title')}</h2>
          <p className="text-center text-muted-foreground">{t('cta.subtitle')}</p>
        </div>

        <div className="flex items-center justify-center gap-2">
          <ShineBorder
            borderRadius={8}
            borderWidth={1}
            duration={8}
            color={["#A07CFE", "#FE8FB5", "#FFBE7B"]}
            className="p-0 bg-transparent dark:bg-transparent"
          >
            <Button onClick={() => setModalOpen(true)} className="bg-black text-white hover:bg-black/90">
              {t('cta.button')}
            </Button>
          </ShineBorder>
        </div>
      </div>

      <ConsultationModal isOpen={modalOpen} onClose={() => setModalOpen(false)} />
    </>
  )
}
```

- [ ] **Step 3: Verify build**

```bash
cd web && npm run build
```

Expected: Build succeeds.

- [ ] **Step 4: Commit**

```bash
cd web && git add src/components/ui/hero.tsx src/components/ui/cta-3.tsx && git commit -m "feat: translate hero and CTA components"
```

---

## Task 7: consultation-modal.tsx

**Files:**
- Modify: `web/src/components/ui/consultation-modal.tsx`

- [ ] **Step 1: Update `web/src/components/ui/consultation-modal.tsx`**

Add `useTranslation` import and replace all hardcoded strings:

```tsx
import { useState, useEffect } from 'react'
import { X, Zap, CheckCircle } from 'lucide-react'
import { useTranslation } from '@/hooks/useTranslation'

interface ConsultationModalProps {
  isOpen: boolean
  onClose: () => void
}

const WEBHOOK_URL = 'https://n8n.vitray.ir/webhook/contact-us-form'

const inputClass =
  'w-full rounded-2xl bg-white/10 border border-white/20 px-4 py-2.5 text-sm text-white placeholder-white/40 outline-none focus:bg-white/20 focus:border-white/40 transition-all duration-200 shadow-lg'

const labelClass = 'block text-xs text-white/60 mb-1'

export function ConsultationModal({ isOpen, onClose }: ConsultationModalProps) {
  const { t, lang } = useTranslation()
  const [form, setForm] = useState({ name: '', email: '', mobile: '', company: '', details: '' })
  const [submitted, setSubmitted] = useState(false)
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState('')

  useEffect(() => {
    const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') onClose() }
    if (isOpen) document.addEventListener('keydown', onKey)
    return () => document.removeEventListener('keydown', onKey)
  }, [isOpen, onClose])

  useEffect(() => {
    document.body.style.overflow = isOpen ? 'hidden' : ''
    return () => { document.body.style.overflow = '' }
  }, [isOpen])

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    setLoading(true)
    setError('')

    const controller = new AbortController()
    const timer = setTimeout(() => controller.abort(), 10000)

    try {
      await fetch(WEBHOOK_URL, {
        method: 'POST',
        headers: { 'Content-Type': 'text/plain' },
        body: JSON.stringify(form),
        mode: 'no-cors',
        signal: controller.signal,
      })
      setSubmitted(true)
    } catch (err) {
      if (err instanceof Error && err.name === 'AbortError') {
        setError(t('modal.error_timeout'))
      } else {
        setError(t('modal.error_generic'))
      }
    } finally {
      clearTimeout(timer)
      setLoading(false)
    }
  }

  const handleClose = () => {
    onClose()
    setTimeout(() => {
      setSubmitted(false)
      setForm({ name: '', email: '', mobile: '', company: '', details: '' })
      setError('')
    }, 300)
  }

  if (!isOpen) return null

  return (
    <div
      className="fixed inset-0 z-50 flex items-center justify-center p-4"
      style={{ backgroundColor: 'rgba(0,0,0,0.7)', backdropFilter: 'blur(6px)' }}
      onClick={(e) => { if (e.target === e.currentTarget) handleClose() }}
    >
      <style>{`
        @keyframes modalIn {
          from { opacity: 0; transform: scale(0.95) translateY(12px); }
          to   { opacity: 1; transform: scale(1)    translateY(0);    }
        }
      `}</style>

      <div
        className="relative w-full max-w-md overflow-hidden rounded-3xl shadow-2xl"
        style={{ animation: 'modalIn 0.28s cubic-bezier(0.16,1,0.3,1)' }}
      >
        <div className="absolute inset-0 z-0">
          <img
            src="https://media.giphy.com/media/xJT7pzbviKNqTqF1Ps/giphy.gif"
            alt=""
            className="w-full h-full object-cover opacity-50"
          />
          <div className="absolute inset-0 bg-gradient-to-b from-blue-600/80 via-blue-900/90 to-black/95" />
        </div>

        <button
          onClick={handleClose}
          className="absolute top-4 right-4 z-20 flex h-8 w-8 items-center justify-center rounded-full bg-white/10 border border-white/20 hover:bg-white/20 transition-colors text-white"
        >
          <X size={14} />
        </button>

        <div className="relative z-10 px-8 py-10">
          {submitted ? (
            <div className="flex flex-col items-center text-center space-y-4 py-6">
              <div className="flex h-14 w-14 items-center justify-center rounded-full bg-white/10 border border-white/20">
                <CheckCircle size={26} className="text-white" />
              </div>
              <h3 className="text-xl font-semibold text-white">{t('modal.success_title')}</h3>
              <p className="text-white/60 text-sm leading-relaxed">{t('modal.success_body')}</p>
              <button
                onClick={handleClose}
                className="mt-2 rounded-2xl bg-white/10 border border-white/20 px-6 py-2 text-sm text-white hover:bg-white/20 transition-colors"
              >
                {t('modal.close')}
              </button>
            </div>
          ) : (
            <>
              <div className="text-center mb-7">
                <div className="w-8 h-8 mx-auto mb-5 text-white">
                  <Zap className="w-full h-full" fill="currentColor" />
                </div>
                <h1 className="text-2xl font-semibold text-white mb-2">{t('modal.title')}</h1>
                <p className="text-white/60 text-sm leading-relaxed">
                  {t('modal.subtitle')}
                  <br />
                  <span className="text-white/80">{t('modal.subtitle2')}</span>
                </p>
              </div>

              <form onSubmit={handleSubmit} className="space-y-3" dir={lang === 'en' ? 'ltr' : 'rtl'}>
                <div className="grid grid-cols-2 gap-3">
                  <div>
                    <label className={labelClass}>{t('modal.field_name')}</label>
                    <input
                      required
                      value={form.name}
                      onChange={e => setForm({ ...form, name: e.target.value })}
                      className={inputClass}
                      placeholder={t('modal.field_name_placeholder')}
                    />
                  </div>
                  <div>
                    <label className={labelClass}>{t('modal.field_mobile')}</label>
                    <input
                      required
                      type="tel"
                      value={form.mobile}
                      onChange={e => setForm({ ...form, mobile: e.target.value })}
                      className={inputClass}
                      placeholder="09123456789"
                      dir="ltr"
                    />
                  </div>
                </div>

                <div>
                  <label className={labelClass}>{t('modal.field_email')}</label>
                  <input
                    required
                    type="email"
                    value={form.email}
                    onChange={e => setForm({ ...form, email: e.target.value })}
                    className={inputClass}
                    placeholder="ali@company.com"
                    dir="ltr"
                  />
                </div>

                <div>
                  <label className={labelClass}>{t('modal.field_company')}</label>
                  <input
                    value={form.company}
                    onChange={e => setForm({ ...form, company: e.target.value })}
                    className={inputClass}
                    placeholder={t('modal.field_company_placeholder')}
                  />
                </div>

                <div>
                  <label className={labelClass}>{t('modal.field_details')}</label>
                  <textarea
                    rows={3}
                    value={form.details}
                    onChange={e => setForm({ ...form, details: e.target.value })}
                    className={`${inputClass} resize-none`}
                    placeholder={t('modal.field_details_placeholder')}
                  />
                </div>

                {error && <p className="text-xs text-red-400">{error}</p>}

                <button
                  type="submit"
                  disabled={loading}
                  className="w-full flex items-center justify-center gap-2 rounded-2xl bg-white/15 border border-white/25 px-6 py-3 text-sm font-semibold text-white hover:bg-white/25 transition-all duration-200 shadow-lg disabled:opacity-50 mt-1"
                >
                  {loading ? t('modal.submitting') : t('modal.submit')}
                </button>
              </form>

              <p className="text-center text-white/40 text-xs mt-5 leading-relaxed">
                {t('modal.privacy')}
              </p>
            </>
          )}
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verify build**

```bash
cd web && npm run build
```

- [ ] **Step 3: Commit**

```bash
cd web && git add src/components/ui/consultation-modal.tsx && git commit -m "feat: translate consultation modal"
```

---

## Task 8: footer-section.tsx

**Files:**
- Modify: `web/src/components/ui/footer-section.tsx`

- [ ] **Step 1: Update `web/src/components/ui/footer-section.tsx`**

Add `useTranslation` import. Replace the hardcoded `footerLinks` array with a function that accepts `t`. Replace all hardcoded strings in JSX.

Key changes:
1. Add `import { useTranslation } from '@/hooks/useTranslation'`
2. Change `const footerLinks: FooterSection[] = [...]` to `function getFooterLinks(t: (k: string) => string): FooterSection[]`
3. Replace all `title:` and `label:` strings with `t('footer.xxx')` calls
4. In `Footer()` component: call `const { t } = useTranslation()` and `const footerLinks = getFooterLinks(t)`
5. Replace the copyright string with `t('footer.copyright')`
6. Replace the address span with `t('footer.address')`

```tsx
import type { ComponentProps, ReactNode } from 'react'
import { motion, useReducedMotion } from 'motion/react'
import { Link } from 'react-router-dom'
import {
  InstagramIcon,
  LinkedinIcon,
  YoutubeIcon,
  Phone,
  MapPin,
  Mail,
} from 'lucide-react'
import { useTranslation } from '@/hooks/useTranslation'

const BiFooterIcon = ({ className }: { className?: string }) => <img src="/fav.png" alt="BI" className={className} />
const PlexFooterIcon = ({ className }: { className?: string }) => <img src="/product%20logos/plex%20fav%20white.png" alt="Plex" className={className} />
const PixelFooterIcon = ({ className }: { className?: string }) => <img src="/product%20logos/Pixel%20Fav%20W.png" alt="Pixel" className={className} />
const PulseFooterIcon = ({ className }: { className?: string }) => <img src="/product%20logos/Pulse%20Fav%20W.png" alt="Pulse" className={className} />
const AcademyFooterIcon = ({ className }: { className?: string }) => <img src="/product%20logos/Academy%20FAV%20White.png" alt="Academy" className={className} />

function AparatIcon({ className }: { className?: string }) {
  return (
    <svg className={className} viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
      <path d="M12 2C6.477 2 2 6.477 2 12s4.477 10 10 10 10-4.477 10-10S17.523 2 12 2zm3.75 10.875l-5.5 3.25A1 1 0 018.75 15.25v-6.5a1 1 0 011.5-.875l5.5 3.25a1 1 0 010 1.75z"/>
    </svg>
  )
}

interface FooterLink { title: string; href: string; icon?: React.ComponentType<{ className?: string }> }
interface FooterSection { label: string; links: FooterLink[] }

function getFooterLinks(t: (k: string) => string): FooterSection[] {
  return [
    {
      label: t('footer.solutions'),
      links: [
        { title: t('footer.bi'), href: '/bi-solution', icon: BiFooterIcon },
        { title: t('footer.plex'), href: '/plex', icon: PlexFooterIcon },
        { title: t('footer.pulse'), href: '/pulse', icon: PulseFooterIcon },
        { title: t('footer.pixel'), href: '/pixel', icon: PixelFooterIcon },
        { title: t('footer.academy'), href: 'https://academy.vitrayco.com', icon: AcademyFooterIcon },
      ],
    },
    {
      label: t('footer.company'),
      links: [
        { title: t('footer.about'), href: '/about' },
        { title: t('footer.contact'), href: '/contact' },
      ],
    },
    {
      label: t('footer.resources'),
      links: [{ title: t('footer.blog'), href: '#blog' }],
    },
    {
      label: t('footer.social'),
      links: [
        { title: t('footer.linkedin'), href: 'https://www.linkedin.com/company/vitrayco/', icon: LinkedinIcon },
        { title: t('footer.instagram'), href: 'https://www.instagram.com/vitrayco/', icon: InstagramIcon },
        { title: t('footer.youtube'), href: 'https://www.youtube.com/@vitraycobi7311', icon: YoutubeIcon },
        { title: t('footer.aparat'), href: 'https://www.aparat.com/VitrayCo.ir', icon: AparatIcon },
      ],
    },
  ]
}

function BrandLogo() {
  return <img src="/Vitray.png" alt="Vitray" className="h-8 w-auto" />
}

export function Footer() {
  const { t } = useTranslation()
  const footerLinks = getFooterLinks(t)

  return (
    <footer className="relative w-full max-w-6xl mx-auto flex flex-col items-center justify-center rounded-t-3xl border-t border-border bg-[radial-gradient(35%_128px_at_50%_0%,rgba(255,255,255,0.08),transparent)] px-6 py-12 lg:py-16">
      <div className="bg-foreground/20 absolute top-0 right-1/2 left-1/2 h-px w-1/3 -translate-x-1/2 -translate-y-1/2 rounded-full blur" />

      <div className="grid w-full gap-8 xl:grid-cols-3 xl:gap-8">
        <AnimatedContainer className="space-y-4">
          <BrandLogo />
          <div className="space-y-3 pt-2">
            <div className="flex items-start gap-2">
              <Phone size={13} className="text-muted-foreground mt-0.5 shrink-0" />
              <a href="tel:+982122865619" className="text-muted-foreground text-sm hover:text-foreground transition-colors" dir="ltr">2286 5619</a>
            </div>
            <div className="flex items-start gap-2">
              <MapPin size={13} className="text-muted-foreground mt-0.5 shrink-0" />
              <span className="text-muted-foreground text-sm leading-relaxed">{t('footer.address')}</span>
            </div>
            <div className="flex items-start gap-2">
              <Mail size={13} className="text-muted-foreground mt-0.5 shrink-0" />
              <span className="text-muted-foreground text-sm" dir="ltr">hello@vitrayco.com</span>
            </div>
          </div>
        </AnimatedContainer>

        <div className="mt-10 grid grid-cols-2 gap-8 md:grid-cols-4 xl:col-span-2 xl:mt-0">
          {footerLinks.map((section, index) => (
            <AnimatedContainer key={section.label} delay={0.1 + index * 0.1}>
              <div className="mb-10 md:mb-0">
                <h3 className="text-xs font-semibold text-foreground/70">{section.label}</h3>
                <ul className="text-muted-foreground mt-4 space-y-2 text-sm">
                  {section.links.map((link) => (
                    <li key={link.title}>
                      {link.href.startsWith('/') && !link.href.startsWith('/#') ? (
                        <Link to={link.href} className="hover:text-foreground inline-flex items-center gap-1.5 transition-all duration-300">
                          {link.icon && <link.icon className="size-3.5 shrink-0" />}
                          {link.title}
                        </Link>
                      ) : (
                        <a
                          href={link.href}
                          {...(link.href.startsWith('http') ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
                          className="hover:text-foreground inline-flex items-center gap-1.5 transition-all duration-300"
                        >
                          {link.icon && <link.icon className="size-3.5 shrink-0" />}
                          {link.title}
                        </a>
                      )}
                    </li>
                  ))}
                </ul>
              </div>
            </AnimatedContainer>
          ))}
        </div>
      </div>

      <div className="mt-12 w-full border-t border-border/40 pt-6 text-center">
        <p className="text-muted-foreground text-sm">
          © {new Date().getFullYear()} {t('footer.copyright')}
        </p>
      </div>
    </footer>
  )
}

type ViewAnimationProps = {
  delay?: number
  className?: ComponentProps<typeof motion.div>['className']
  children: ReactNode
}

function AnimatedContainer({ className, delay = 0.1, children }: ViewAnimationProps) {
  const shouldReduceMotion = useReducedMotion()
  if (shouldReduceMotion) return <>{children}</>
  return (
    <motion.div
      initial={{ filter: 'blur(4px)', translateY: -8, opacity: 0 }}
      whileInView={{ filter: 'blur(0px)', translateY: 0, opacity: 1 }}
      viewport={{ once: true }}
      transition={{ delay, duration: 0.8 }}
      className={className}
    >
      {children}
    </motion.div>
  )
}
```

- [ ] **Step 2: Verify build**

```bash
cd web && npm run build
```

- [ ] **Step 3: Commit**

```bash
cd web && git add src/components/ui/footer-section.tsx && git commit -m "feat: translate footer component"
```

---

## Task 9: feature-section-with-card-gradient.tsx

**Files:**
- Modify: `web/src/components/ui/feature-section-with-card-gradient.tsx`

The `dashboards` array needs to become a function that accepts `t()`, so dashboard cards translate. The grid patterns and hrefs are unchanged.

- [ ] **Step 1: Update `web/src/components/ui/feature-section-with-card-gradient.tsx`**

Replace the `dashboards` const array with a `getDashboards(t)` function, update `DashboardTemplatesSection` and `RelatedDashboardsSection` to call `useTranslation`:

```tsx
import React, { useId } from "react"
import { Link } from "react-router-dom"
import { useTranslation } from "@/hooks/useTranslation"

interface DashboardItem {
  title: string
  description: string
  href: string
  pattern: number[][]
}

function getDashboards(t: (k: string) => string): DashboardItem[] {
  return [
    { title: t('dashboards.b2b_title'), description: t('dashboards.b2b_desc'), href: "/bi-dashboards/b2b-sales", pattern: [[8,1],[9,3],[11,2],[10,4],[8,5]] },
    { title: t('dashboards.dist_title'), description: t('dashboards.dist_desc'), href: "/bi-dashboards/distribution-sales", pattern: [[7,2],[10,1],[8,4],[9,5],[11,3]] },
    { title: t('dashboards.finance_title'), description: t('dashboards.finance_desc'), href: "/bi-dashboards/finance", pattern: [[9,1],[8,3],[11,5],[10,2],[7,4]] },
    { title: t('dashboards.hr_title'), description: t('dashboards.hr_desc'), href: "/bi-dashboards/hr", pattern: [[10,2],[7,1],[9,4],[11,3],[8,5]] },
    { title: t('dashboards.warehouse_title'), description: t('dashboards.warehouse_desc'), href: "/bi-dashboards/warehouse", pattern: [[8,2],[11,1],[7,3],[10,5],[9,4]] },
    { title: t('dashboards.production_title'), description: t('dashboards.production_desc'), href: "/bi-dashboards/production", pattern: [[9,2],[8,4],[11,1],[7,5],[10,3]] },
    { title: t('dashboards.maintenance_title'), description: t('dashboards.maintenance_desc'), href: "/bi-dashboards/maintenance", pattern: [[10,1],[8,3],[11,4],[7,2],[9,5]] },
  ]
}

export function DashboardTemplatesSection() {
  const { t } = useTranslation()
  const dashboards = getDashboards(t)

  return (
    <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
      {dashboards.map((item) => (
        <Link
          key={item.href}
          to={item.href}
          className="relative bg-gradient-to-b from-neutral-900 to-neutral-950 p-6 rounded-3xl overflow-hidden border border-border/60 hover:border-primary/40 transition-colors group"
        >
          <Grid size={20} pattern={item.pattern} />
          <p className="text-base font-bold text-white relative z-20 group-hover:text-primary transition-colors">{item.title}</p>
          <p className="text-neutral-400 mt-4 text-sm font-normal relative z-20 leading-relaxed">{item.description}</p>
        </Link>
      ))}
    </div>
  )
}

export function RelatedDashboardsSection({ currentHref }: { currentHref: string }) {
  const { t } = useTranslation()
  const others = getDashboards(t).filter((d) => d.href !== currentHref)

  return (
    <div className="mx-auto max-w-5xl px-6 py-16">
      <div className="flex flex-col items-center justify-center max-w-[540px] mx-auto text-center space-y-4 mb-12">
        <div className="border border-border/60 py-1 px-4 rounded-lg text-sm text-muted-foreground w-fit">
          {t('dashboards.related_badge')}
        </div>
        <h2 className="text-2xl sm:text-3xl md:text-4xl font-bold tracking-tighter">
          {t('dashboards.related_title')}
        </h2>
        <p className="text-muted-foreground text-sm md:text-base">
          {t('dashboards.related_body')}
        </p>
      </div>
      <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
        {others.map((item) => (
          <Link
            key={item.href}
            to={item.href}
            className="relative bg-gradient-to-b from-neutral-900 to-neutral-950 p-6 rounded-3xl overflow-hidden border border-border/60 hover:border-primary/40 transition-colors group"
          >
            <Grid size={20} pattern={item.pattern} />
            <p className="text-base font-bold text-white relative z-20 group-hover:text-primary transition-colors">{item.title}</p>
            <p className="text-neutral-400 mt-4 text-sm font-normal relative z-20 leading-relaxed">{item.description}</p>
          </Link>
        ))}
      </div>
    </div>
  )
}

export const Grid = ({ pattern, size }: { pattern?: number[][]; size?: number }) => {
  return (
    <div className="pointer-events-none absolute left-1/2 top-0 -ml-20 -mt-2 h-full w-full [mask-image:linear-gradient(white,transparent)]">
      <div className="absolute inset-0 bg-gradient-to-r [mask-image:radial-gradient(farthest-side_at_top,white,transparent)] from-zinc-900/30 to-zinc-900/30 opacity-100">
        <GridPattern width={size ?? 20} height={size ?? 20} x="-12" y="4" squares={pattern} className="absolute inset-0 h-full w-full mix-blend-overlay fill-white/10 stroke-white/10" />
      </div>
    </div>
  )
}

export function GridPattern({ width, height, x, y, squares, ...props }: { width: number; height: number; x: string; y: string; squares?: number[][]; className?: string }) {
  const patternId = useId()
  return (
    <svg aria-hidden="true" {...props}>
      <defs>
        <pattern id={patternId} width={width} height={height} patternUnits="userSpaceOnUse" x={x} y={y}>
          <path d={`M.5 ${height}V.5H${width}`} fill="none" />
        </pattern>
      </defs>
      <rect width="100%" height="100%" strokeWidth={0} fill={`url(#${patternId})`} />
      {squares && (
        <svg x={x} y={y} className="overflow-visible">
          {squares.map(([sx, sy]) => (
            <rect strokeWidth="0" key={`${sx}-${sy}`} width={width + 1} height={height + 1} x={sx * width} y={sy * height} />
          ))}
        </svg>
      )}
    </svg>
  )
}
```

- [ ] **Step 2: Verify build**

```bash
cd web && npm run build
```

- [ ] **Step 3: Commit**

```bash
cd web && git add src/components/ui/feature-section-with-card-gradient.tsx && git commit -m "feat: translate dashboard cards component"
```

---

## Task 10: plex-hero.tsx, pixel-hero.tsx, pulse-hero.tsx

**Files:**
- Modify: `web/src/components/ui/plex-hero.tsx`
- Modify: `web/src/components/ui/pixel-hero.tsx`
- Modify: `web/src/components/ui/pulse-hero.tsx`

> **Note:** Before starting, read `pixel-hero.tsx` and `pulse-hero.tsx` to capture their Persian strings, then add the missing keys to `fa.json`/`en.json` under `"pixel_hero"` and `"pulse_hero"` namespaces following the same pattern as `"plex_hero"`.

- [ ] **Step 1: Read pixel-hero.tsx and pulse-hero.tsx**

```bash
cat web/src/components/ui/pixel-hero.tsx
cat web/src/components/ui/pulse-hero.tsx
```

- [ ] **Step 2: Add missing keys to `fa.json` and `en.json`**

Add `"pixel_hero"` and `"pulse_hero"` namespaces to both locale files following the same pattern as `"plex_hero"`, with creative English translations matching the Pixel/Pulse brand voice.

- [ ] **Step 3: Update `web/src/components/ui/plex-hero.tsx`**

```tsx
import { MeshGradient } from "@paper-design/shaders-react"
import { motion } from "framer-motion"
import { useTranslation } from "@/hooks/useTranslation"

export function PlexHero() {
  const { t } = useTranslation()

  return (
    <div className="relative w-full overflow-hidden" style={{ height: "100vh", minHeight: 520 }}>
      <MeshGradient
        className="absolute inset-0 w-full h-full"
        colors={["#000000", "#1d4ed8", "#1e40af", "#0f172a", "#0284c7"]}
        speed={0.3}
        backgroundColor="#000000"
      />
      <MeshGradient
        className="absolute inset-0 w-full h-full opacity-40"
        colors={["#000000", "#ffffff", "#1d4ed8", "#0284c7"]}
        speed={0.2}
        wireframe="true"
        backgroundColor="transparent"
      />
      <div
        aria-hidden="true"
        className="pointer-events-none absolute bottom-0 left-0 right-0 h-48 z-10"
        style={{ background: "linear-gradient(to bottom, transparent, hsl(var(--background)))" }}
      />
      <div className="absolute inset-0 flex items-center justify-center z-20 px-6">
        <motion.div
          initial={{ opacity: 0, y: 24 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.9, delay: 0.15, ease: [0.16, 1, 0.3, 1] }}
          className="flex flex-col items-center max-w-[640px] text-center space-y-5"
        >
          <div className="inline-flex items-center gap-2 border border-white/20 bg-white/5 backdrop-blur-sm py-1 px-4 rounded-lg text-sm text-white/70 w-fit">
            {t('plex_hero.badge')}
          </div>
          <h1 className="text-5xl md:text-7xl font-bold tracking-tighter text-white leading-tight">
            {t('plex_hero.title1')}
            <br />
            {t('plex_hero.title2')}
          </h1>
          <p className="text-white/60 text-sm md:text-base leading-relaxed max-w-md">
            {t('plex_hero.body')}
          </p>
        </motion.div>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Update pixel-hero.tsx and pulse-hero.tsx following the same pattern**

Use `t('pixel_hero.badge')`, `t('pixel_hero.title1')` etc. Keep all styling/animation identical — only replace hardcoded text.

- [ ] **Step 5: Verify build**

```bash
cd web && npm run build
```

- [ ] **Step 6: Commit**

```bash
cd web && git add src/components/ui/plex-hero.tsx src/components/ui/pixel-hero.tsx src/components/ui/pulse-hero.tsx src/locales/ && git commit -m "feat: translate product hero components"
```

---

## Task 11: App.tsx (Home Page)

**Files:**
- Modify: `web/src/App.tsx`

- [ ] **Step 1: Add missing home section keys to both locale files**

The following keys are needed for App.tsx sections not yet in the locale files. Add them to the `"home"` namespace in both `fa.json` and `en.json`:

```json
// Add to fa.json "home":
"academy_link": "ورود به آکادمی",
"testimonials_badge": "نظرات مشتریان",
"testimonials_title": "مشتریان ما چه می‌گویند",
"testimonials_body": "تجربه واقعی کسب‌وکارهایی که با راهکارهای ما رشد کرده‌اند.",
"why_diff_title": "چه چیزی ما را متفاوت می‌کند",
"why_diff_body": "چهار دلیل اصلی که سازمان‌ها ویترای را به‌عنوان شریک داده‌ای خود انتخاب می‌کنند.",
"maturity_badge": "مدل بلوغ داده",
"maturity_title": "همراه شما از آگاهی تا داده‌محوری",
"maturity_body": "حرکت به‌سوی تصمیم‌گیری مبتنی بر داده سفری تدریجی و هدفمند است. در ویترای، رویکرد ما بر پایه‌ی مدل بلوغ داده طراحی شده — مدلی که مسیر حرکت سازمان‌ها از آگاهی اولیه تا تصمیم‌گیری کاملاً داده‌محور را ترسیم می‌کند.",
"stage1_title": "آگاهی از داده",
"stage1_body": "داده‌ها به‌صورت دستی از منابع مختلف گردآوری می‌شوند. تمرکز بر ایجاد درک اولیه از ارزش داده و استانداردسازی گزارش‌هاست.",
"stage2_title": "تسلط بر داده",
"stage2_body": "سازمان از یک پلتفرم متمرکز برای گزارش‌دهی استفاده می‌کند و شاخص‌های کلیدی عملکرد خود را به شکل منظم پایش می‌کند.",
"stage3_title": "هوشمندی داده",
"stage3_body": "داده‌ها به منبع اصلی تصمیم‌گیری‌های کلیدی تبدیل می‌شوند و تحلیل‌ها نقش تعیین‌کننده در جهت‌گیری‌های سازمان دارند.",
"stage4_title": "داده‌محوری",
"stage4_body": "داده در تمامی فرآیندها و تصمیمات سازمان نهادینه می‌شود و تصمیمی بدون اتکا به داده گرفته نمی‌شود."
```

```json
// Add to en.json "home":
"academy_link": "Go to Academy",
"testimonials_badge": "Client Stories",
"testimonials_title": "What Our Clients Say",
"testimonials_body": "Real experiences from businesses that have grown with our solutions.",
"why_diff_title": "What Makes Us Different",
"why_diff_body": "Four reasons organizations choose Vitray as their data partner.",
"maturity_badge": "Data Maturity Model",
"maturity_title": "With You From Awareness to Data-Driven",
"maturity_body": "The journey to data-driven decision-making is gradual and purposeful. At Vitray, our approach is designed around the data maturity model — mapping the path from initial awareness to fully data-driven decision-making.",
"stage1_title": "Data Aware",
"stage1_body": "Data is collected manually from multiple sources. The focus is on building initial understanding of data's value and standardizing reports.",
"stage2_title": "Data Proficient",
"stage2_body": "The organization uses a centralized platform for reporting and regularly monitors its key performance indicators.",
"stage3_title": "Data Savvy",
"stage3_body": "Data becomes the primary source of key decisions, with analytics playing a decisive role in organizational direction.",
"stage4_title": "Data Driven",
"stage4_body": "Data is embedded in every process and decision across the organization — no decision is made without it."
```

- [ ] **Step 2: Update `web/src/App.tsx`**

Key changes:
1. Add `import { useTranslation } from '@/hooks/useTranslation'`
2. Add `import { getNavItems } from '@/lib/navItems'`
3. Remove the hardcoded `navItems` and `whyItems` arrays from module scope
4. Inside `App()`, add `const { t } = useTranslation()` then build nav and content arrays

```tsx
import { useTranslation } from '@/hooks/useTranslation'
import { getNavItems } from '@/lib/navItems'
// ... all other existing imports unchanged

// REMOVE the module-level navItems and whyItems arrays

export function App() {
  usePageTitle()
  const { t } = useTranslation()

  // Override hash URLs for home page scroll behavior
  const navItems = getNavItems(t).map(item => {
    if (item.url === '/') return { ...item, url: '#home' }
    if (item.url === '/#products') return { ...item, url: '#products' }
    return item
  })

  const whyItems = [
    { icon: <Briefcase className="w-4 h-4 text-primary" />, title: t('home.why_business'), description: t('home.why_business_body'), hasPersistentHover: true },
    { icon: <TrendingUp className="w-4 h-4 text-primary" />, title: t('home.why_speed'), description: t('home.why_speed_body') },
    { icon: <Target className="w-4 h-4 text-primary" />, title: t('home.why_customer'), description: t('home.why_customer_body') },
    { icon: <Lightbulb className="w-4 h-4 text-primary" />, title: t('home.why_tools'), description: t('home.why_tools_body') },
  ]

  const maturityStages = [
    { icon: Database, title: t('home.stage1_title'), en: 'Data Aware', body: t('home.stage1_body') },
    { icon: BarChart2, title: t('home.stage2_title'), en: 'Data Proficient', body: t('home.stage2_body') },
    { icon: Lightbulb, title: t('home.stage3_title'), en: 'Data Savvy', body: t('home.stage3_body') },
    { icon: Target, title: t('home.stage4_title'), en: 'Data Driven', body: t('home.stage4_body') },
  ]

  return (
    <div className="min-h-screen bg-background text-foreground">
      <NavBar items={navItems} />
      <div id="home"><PremiumHero /></div>

      <main className="mx-auto max-w-5xl px-6 py-12">
        {/* Customers section */}
        <section id="customers" className="space-y-8">
          {/* badge */} <div ...>{t('home.customers_badge')}</div>
          {/* h2 */} <h2 ...>{t('home.customers_title')}</h2>
          {/* p */} <p ...>{t('home.customers_body')}</p>
          <LogoCarousel ... />
        </section>

        {/* Products section */}
        <section id="products" className="space-y-8">
          {/* badge */} <div ...>{t('home.solutions_badge')}</div>
          {/* h2 */} <h2 ...>{t('home.solutions_title')}</h2>
          {/* p */} <p ...>{t('home.solutions_body')}</p>

          {/* BI card */}
          <GlowCard ...>
            <div ...>{t('home.bi_badge')}</div>
            <h3>{t('home.bi_title')}</h3>
            <p>{t('home.bi_body')}</p>
            <Link to="/bi-solution">{t('home.learn_more')}</Link>
          </GlowCard>

          {/* Plex card: badge=home.plex_badge, title=home.plex_title, body=home.plex_body, link=home.learn_more */}
          {/* Pulse card: badge=home.pulse_badge, title=home.pulse_title, body=home.pulse_body, link=home.learn_more */}
          {/* Pixel card: badge=home.pixel_badge, title=home.pixel_title, body=home.pixel_body, link=home.learn_more */}

          {/* Academy card */}
          <GlowCard ...>
            <div ...>{t('home.academy_badge')}</div>
            <h3>{t('home.academy_title')}</h3>
            <p>{t('home.academy_body')}</p>
            <a href="https://academy.vitrayco.com" ...>{t('home.academy_link')}</a>
          </GlowCard>
        </section>

        {/* Testimonials section */}
        <section id="testimonials" className="space-y-8">
          <div ...>{t('home.testimonials_badge')}</div>
          <h2 ...>{t('home.testimonials_title')}</h2>
          <p ...>{t('home.testimonials_body')}</p>
          <FocusCards cards={customerSuccessCards} />
          {/* customerSuccessCards titles are proper company names — do NOT translate them */}
        </section>

        {/* Why Vitray section */}
        <section className="space-y-8">
          <div ...>{t('home.why_badge')}</div>
          <h2 ...>{t('home.why_diff_title')}</h2>
          <p ...>{t('home.why_diff_body')}</p>
          <BentoGrid items={whyItems} />
        </section>

        {/* Data Maturity section */}
        <section className="space-y-8">
          <div ...>{t('home.maturity_badge')}</div>
          <h2 ...>{t('home.maturity_title')}</h2>
          <p ...>{t('home.maturity_body')}</p>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 ...">
            {maturityStages.map((stage) => (
              <div key={stage.title} ...>
                <stage.icon ... />
                <span>{stage.title}</span>
                <p>{stage.body}</p>
                <span dir="ltr">{stage.en}</span>
                {/* stage.en values are English labels — keep them hardcoded */}
              </div>
            ))}
          </div>
        </section>
      </main>

      <CallToAction />
      <Footer />
    </div>
  )
}
```

> **Important:** The `customerSuccessCards` titles are proper company names — do not translate them. The `customerLogos` `name` fields are already in English — leave them. The `stage.en` maturity level labels are English by design in both languages.

- [ ] **Step 2: Verify build**

```bash
cd web && npm run build
```

Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
cd web && git add src/App.tsx && git commit -m "feat: translate home page (App.tsx)"
```

---

## Task 12: AboutPage + ContactPage

**Files:**
- Modify: `web/src/pages/AboutPage.tsx`
- Modify: `web/src/pages/ContactPage.tsx`

- [ ] **Step 1: Update `web/src/pages/AboutPage.tsx`**

1. Add `import { useTranslation } from '@/hooks/useTranslation'`
2. Add `import { getNavItems } from '@/lib/navItems'`
3. Remove the hardcoded `navItems` array
4. Inside `AboutPage()`, add `const { t } = useTranslation()` and `const navItems = getNavItems(t)`
5. Replace `usePageTitle('درباره ما')` with `usePageTitle('page_titles.about')`
6. Replace all hardcoded Persian strings with `t('about.xxx')` calls

Stat values (`'۱۳۹۸'`, `'+150'`) are data — keep them as-is.

- [ ] **Step 2: Update `web/src/pages/ContactPage.tsx`**

1. Add `import { useTranslation } from '@/hooks/useTranslation'`
2. Add `import { getNavItems } from '@/lib/navItems'`
3. Remove the hardcoded `navItems` array
4. Inside `ContactPage()`, add `const { t } = useTranslation()` and `const navItems = getNavItems(t)`
5. Replace `usePageTitle('تماس با ما')` with `usePageTitle('page_titles.contact')`
6. Replace all hardcoded Persian strings with `t('contact.xxx')` calls

> Phone number (`2286 5619`) and email (`hello@vitrayco.com`) are data — keep `dir="ltr"` and leave the values as-is.

- [ ] **Step 3: Verify build**

```bash
cd web && npm run build
```

- [ ] **Step 4: Commit**

```bash
cd web && git add src/pages/AboutPage.tsx src/pages/ContactPage.tsx && git commit -m "feat: translate About and Contact pages"
```

---

## Task 13: BiSolutionPage

**Files:**
- Modify: `web/src/pages/BiSolutionPage.tsx`

- [ ] **Step 1: Read BiSolutionPage.tsx to capture all Persian strings**

```bash
cat web/src/pages/BiSolutionPage.tsx
```

- [ ] **Step 2: Add `"bi_solution"` keys to both locale files**

Add `"bi_solution": { ... }` namespace to `fa.json` and `en.json` with all the strings found in the page.

- [ ] **Step 3: Update `web/src/pages/BiSolutionPage.tsx`**

Same pattern: add `useTranslation` + `getNavItems`, remove hardcoded `navItems`, update `usePageTitle` call, replace all Persian strings with `t()` calls.

- [ ] **Step 4: Verify build**

```bash
cd web && npm run build
```

- [ ] **Step 5: Commit**

```bash
cd web && git add src/pages/BiSolutionPage.tsx src/locales/ && git commit -m "feat: translate BI Solution page"
```

---

## Task 14: PlexPage, PixelPage, PulsePage

**Files:**
- Modify: `web/src/pages/PlexPage.tsx`
- Modify: `web/src/pages/PixelPage.tsx`
- Modify: `web/src/pages/PulsePage.tsx`

- [ ] **Step 1: Read all three pages**

```bash
cat web/src/pages/PlexPage.tsx
cat web/src/pages/PixelPage.tsx
cat web/src/pages/PulsePage.tsx
```

- [ ] **Step 2: Add missing keys to locale files**

Add keys for `"plex"`, `"pixel"`, `"pulse"` namespaces to both `fa.json` and `en.json`. For each page, capture: capability cards (icon, title, body), feature highlights, section headings.

- [ ] **Step 3: Update all three pages**

Same pattern for each:
1. Add `useTranslation` + `getNavItems` imports
2. Remove hardcoded `navItems`
3. Update `usePageTitle` call
4. Move `capabilities` and other content arrays inside the component (so they call `t()`)
5. Replace all hardcoded Persian strings

- [ ] **Step 4: Verify build**

```bash
cd web && npm run build
```

- [ ] **Step 5: Commit**

```bash
cd web && git add src/pages/PlexPage.tsx src/pages/PixelPage.tsx src/pages/PulsePage.tsx src/locales/ && git commit -m "feat: translate Plex, Pixel, Pulse product pages"
```

---

## Task 15: Dashboard Pages (all 7)

**Files:**
- Modify: all 7 files in `web/src/pages/dashboards/`

All dashboard pages follow the identical pattern — they each have:
- The same `navItems` array (duplicate)
- A `usePageTitle` call
- A single `<h1>` with the dashboard name

- [ ] **Step 1: Update all 7 dashboard pages**

For each of the 7 files (`FinanceDashboardPage.tsx`, `HRDashboardPage.tsx`, `WarehouseDashboardPage.tsx`, `MaintenanceDashboardPage.tsx`, `ProductionDashboardPage.tsx`, `DistributionSalesDashboardPage.tsx`, `B2BSalesDashboardPage.tsx`):

1. Add `import { useTranslation } from '@/hooks/useTranslation'`
2. Add `import { getNavItems } from '@/lib/navItems'`
3. Remove the hardcoded `navItems` array
4. Inside the component, add `const { t } = useTranslation()` and `const navItems = getNavItems(t)`
5. Replace `usePageTitle('داشبورد مالی')` with `usePageTitle('page_titles.dashboard_finance')` (and corresponding keys for each page)
6. Replace the `<h1>` hardcoded text with `{t('page_titles.dashboard_finance')}` (same key)

Example for `FinanceDashboardPage.tsx`:
```tsx
import { NavBar } from '@/components/ui/tubelight-navbar'
import { Footer } from '@/components/ui/footer-section'
import { RelatedDashboardsSection } from '@/components/ui/feature-section-with-card-gradient'
import { usePageTitle } from '@/lib/usePageTitle'
import { useTranslation } from '@/hooks/useTranslation'
import { getNavItems } from '@/lib/navItems'

export function FinanceDashboardPage() {
  const { t } = useTranslation()
  const navItems = getNavItems(t)
  usePageTitle('page_titles.dashboard_finance')

  return (
    <div className="min-h-screen bg-background text-foreground">
      <NavBar items={navItems} />
      <div className="flex items-center justify-center min-h-[80vh]">
        <h1 className="text-4xl md:text-6xl font-bold tracking-tighter">{t('page_titles.dashboard_finance')}</h1>
      </div>
      <RelatedDashboardsSection currentHref="/bi-dashboards/finance" />
      <Footer />
    </div>
  )
}
```

Apply the same pattern to all 7 dashboard pages with their respective keys.

- [ ] **Step 2: Verify build**

```bash
cd web && npm run build
```

- [ ] **Step 3: Commit**

```bash
cd web && git add src/pages/dashboards/ && git commit -m "feat: translate all 7 dashboard pages"
```

---

## Task 16: Final Verification

- [ ] **Step 1: Run full test suite**

```bash
cd web && npm test
```

Expected: All tests PASS.

- [ ] **Step 2: Run production build**

```bash
cd web && npm run build
```

Expected: Build succeeds with no errors or warnings.

- [ ] **Step 3: Run dev server and manually verify**

```bash
cd web && npm run dev
```

Open the site in a browser and verify:
- [ ] Default language is Persian (RTL layout, IRANSans font)
- [ ] Clicking EN/فا button in navbar toggles to English (LTR layout, system font)
- [ ] Language preference survives page reload
- [ ] Language switcher appears on mobile
- [ ] All pages render without any untranslated keys appearing (no `"nav.home"` visible in UI)
- [ ] Page titles in browser tab switch correctly
- [ ] Consultation modal translates correctly
- [ ] Footer links and labels translate correctly

- [ ] **Step 4: Final commit**

```bash
cd web && git add -A && git commit -m "feat: complete English language support with RTL/LTR switching"
```
